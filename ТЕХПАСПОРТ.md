# ТЕХПАСПОРТ — Симулятор 12-канальной ЭКГ

**Версия:** v18.x · **Движок:** Godot 4.7 / GDScript · **Платформа:** мобильная (Android/iOS), десктоп
**Назначение:** клинически-корректный обучающий симулятор ЭКГ. Морфология рождается из
физической модели сердца (не таблицы подстановки). Тренажёр для распознавания паттернов.

> ⚠️ Не медицинское изделие. Не для диагностики реальных пациентов — учебный тренажёр.

---

## 1. Архитектура

Чистое разделение «движок ↔ UI». Движок — статические функции без состояния.

| Модуль | Строк | Роль |
|---|---|---|
| `ecg_model.gd` (`ECGModel`) | ~1280 | Движок: 17 сегментов миокарда, активация по Дейкстре (His-Пуркинье), гибридная модель (тайминг сегментов → нетто-векторы фаз QRS), T по градиенту Вилсона, ST 3D-вектор, генерация событий ритма, рендер отведений, диагностика, нагрузочная проба, переходы терапии |
| `main.gd` (`Control`) | ~1784 | UI: 6 вкладок, эффективные параметры, препараты, пробы, викторина/экзамен, темы, динамика терапии |
| `monitor_view.gd` (`MonitorView`) | ~135 | Бегущий монитор: sweep-развёртка, ЧСС, R-точки, переходы |
| `lead_view.gd` (`LeadView`) | ~46 | Миниатюра отведения |
| `edit_view.gd` (`EditView`) | ~181 | Интерактивная правка кривой пальцем по сетке ЭКГ |
| `axis_view.gd` / `stress_view.gd` | ~38 / 68 | Диаграмма оси / тренд нагрузочной пробы |
| `ecg_sheet.gd` (`ECGSheet`) | ~80 | Рендер 12-канального листа для экспорта в PNG |

**Система координат:** X — влево, Y — вниз, Z — назад. Окно 4000 мс / 800 отсчётов (5 мс/отсчёт).
12 осей отведений в `LEADVEC` (фронтальная гексаксиальная + поперечная грудная).

---

## 2. Физическая модель (движок)

- **Проводящая система:** активация 17 сегментов алгоритмом Дейкстры от эндокардиальных
  точек впрыска His-Пуркинье; скорость по миокарду `V_MUSCLE`. Блокады (ЛНПГ/ПНПГ/гемиблоки)
  — отключение соответствующих точек впрыска → эмерджентная ширина и форма QRS.
- **QRS:** тайминг фаз из активации сегментов → калиброванные нетто-векторы (септум, главный
  вектор ЛЖ, ПЖ, базальный) как гауссианы.
- **T:** желудочковый градиент Вилсона по реполяризации (активация + APD сегмента); вторичные
  дискордантные T при блокадах/перегрузке. Нормальный T **асимметричен** (пологий подъём,
  крутой спад — полугауссиан); при ишемии/перегрузке T симметричен (клинически значимо).
- **ST:** единый 3D-вектор повреждения; форма сегмента (выпуклый/корытообразный).
- **Прогрессия R V1→V6 + калибровка оси:** передне-левая «переходная» R-компонента
  (`qrs_axis` 60°→измеренная ~59°).

**Методология валидации:** каждая морфология/формула проверена в Python (NumPy/Matplotlib) —
сетка 12 отведений и сверка с клиническим эталоном до переноса в GDScript.

---

## 3. Клинический контент

### Ритмы (21)
синусовый, ФП, **трепетание предсердий** (пилообразные F, проведение N:1), **АВУРТ**,
**узловой (АВ-узловой) ритм**, **ускоренный идиовентрикулярный (AIVR)** (АВ-диссоциация),
ЖТ, **Torsades**, **двунаправленная ЖТ**, ФЖ (крупно-/мелковолновая), **ЭМД/PEA**,
**СССУ/паузы**, AV-блокада 2 ст. **Мобитц I (Венкебах)** и **Мобитц II**, полная
AV-блокада, **асистолия**, ЭКС **VVI/AAI/DDD/BiV**.

### Нарушения проводимости
ПНПГ (rSR′ + дискордантная инверсия T V1-V3), ПЛНПГ (дискордантный T ∝ QRS),
**гемиблоки ЛПВ/ЛЗВ**, **бифасцикулярная** (ПНПГ+ЛПВ), **трифасцикулярная** (+PR).

### Ишемия/инфаркт
STEMI нижний / передний (QS) / **задний** (зеркало V1-V2); выпуклый «надгробный» ST,
патологический Q территориально из вектора повреждения, реципрокные изменения.

### Прочее
ГЛЖ/ГПЖ с **перегрузкой (strain)**; **перегрузка ПЖ / паттерн ТЭЛА** (тахи + RAD +
инверсия T V1-V3); электролиты (гипер/гипо-K с U-волной и шатровым T, гипер/гипо-Ca с
коротким/длинным QT); синдромы — **WPW** (дельта), **Wellens**, **ранняя реполяризация**,
**гипотермия (волны Осборна)**, **перикардит** (диффузный ST + депрессия PR), **Бругада
тип 1**; неисправности ЭКС (потеря захвата, undersensing).

### Препараты (15)
антиаритмики I-IV классов, дигоксin, атропин, адреналин, аденозин, магний, ТЦА,
электролит-активные — терапевтическая/токсическая дозы с дозозависимыми эффектами.

Аберрантное проведение: **феномен Ашмана** при ФП (цикл «длинный-короткий RR» →
комплекс с картиной БПНПГ, имитирует ЖЭ). **Электрическая альтернация** (перикардиальный
выпот/тампонада: амплитуда QRS-T чередуется через удар + качание оси, низкий вольтаж).

**Пресетов:** 50.

---

## 4. Динамика терапии (живой монитор)

- **Купирование аритмий** антиаритмиками → переход аритмия→синус (анимация + баннер).
- **Индукция токсичностью** (дигоксин→двунапр. ЖТ и т.д.) → анимация синус→аритмия.
- **Дефибрилляция/кардиоверсия** — артефакт разряда → синус; **асистолия не шоковая**
  (нет показаний); при лекарственной причине — рецидив.
- **Реанимация:** асистолия + **адреналин** → ROSC; **атропин** при брадикардии/AV-блокаде.

---

## 5. Интерфейс

- **6 вкладок:** Монитор · Параметры · Препараты · Анализ · Проба · Викторина.
- **Выбор патологии/ритма:** каскадные зависимые списки (категория → отфильтрованный
  список) + шаговые стрелки ◀ ▶ для перебора по тапу; прокрутка вкладок протяжкой пальца.
- **Монитор:** sweep-развёртка со стирающим курсором, 25/50 мм/с, ЧСС-цифра, точки R,
  переходы терапии, экспорт листа в PNG.
- **Правка кривой (2D):** маркеры P/Q/R/S/T/ST тянутся пальцем по миллиметровке —
  вертикаль = амплитуда, горизонталь = время (ширина P, PR, ширина QRS, QT); инфаркт
  (ST) правится по уровню. Обратная подгонка параметров модели вживую.
- **Линейка (калиперы):** две вертикальные каретки на сетке ЭКГ измеряют интервал
  (Δt), мгновенную ЧСС и амплитуду (ΔU) между точками — переключается с режима правки.
- **Нагрузочная проба:** ступени, ЧСС/ST/АД, тренд, заключение, ЖЭ на пике.
- **Викторина:** загадка с бегущей лентой + 12 отведений, 4 варианта, уровни (новичок/
  эксперт), фильтр по системам; **режим экзамена** (10 вопросов → оценка + разбор);
  счёт и рекорд сохраняются.
- **Темы:** синяя / зелёная / светлая (сохраняются между запусками); скруглённые карточки,
  акцентные заголовки, стилизованные скроллбары/чекбоксы.

**Сетка ЭКГ:** 25 мм/с · 10 мм/мВ (клетка 0.04 с / 0.1 мВ, крупная 0.2 с / 0.5 мВ).

---

## 6. Диагностический блок

Единые клинические пороги (`const THR`): ЧСС 100/60, ST ±1.0 мм, QRS 120 мс, QTc 460,
PR 200, K/Ca/T — используются в классификации, дифф-диагнозе (сигмоидные вероятности),
оценке нормы и самочувствии. Соколов-Лайон (ГЛЖ/ГПЖ), измеренная электрическая ось,
QTc по Базетту, текстовое заключение. **Критерии Сгарбоссы** (ОКС на фоне ЛБНПГ/ЭКС:
конкордантный подъём / депрессия V1-V3 / чрезмерная дискордантность ≥5 мм, балл ≥3).
**Строка-пояснение** «как отличить · почему» выводится из текущего состояния модели.

---

## 7. Сборка и запуск

1. Godot **4.7** → Import → выбрать `project.godot` → F5.
2. Точка входа — `main.tscn` (узел `Main`, скрипт `main.gd`).
3. Экспорт ЭКГ: Монитор → «Экспорт в PNG» → `user://ecg_export.png`.
4. Настройки (тема, счёт викторины) — `user://settings.cfg`.

**Состав:** 8 скриптов `.gd`, `main.tscn`, `project.godot`, `icon.svg`, документация.
~3600 строк GDScript.

## 8. Жёсткие правила кода (Godot 4.7)
4 пробела (без табов); нет повторного `var` в одной функции; `OptionButton.selected`
не эмитит сигнал; `Range.set_value_no_signal()` для тихой синхронизации; строковый
`in` — подстрока, `begins_with()` для префиксов; типобезопасные `:=` (Variant из
словарей оборачивать `float()`); линтер перед каждой сборкой.
